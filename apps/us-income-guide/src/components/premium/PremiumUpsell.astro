---

---

<aside class="premium-upsell" aria-label="付费资源推荐">
	<div class="inner">
		<p class="lead">需要完整工具包？</p>
		<p class="sub">
			付费资源库提供可下载模板、清单与工具包（未来将配合支付与账号系统上线）。当前仅为产品原型展示，<strong>不涉及真实扣款</strong>。
		</p>
		<a class="btn" href="/premium/">查看付费资源库</a>
		<p class="note">
			不保证收入结果。详见 <a href="/earnings-disclosure/">收入声明说明</a>。
		</p>
	</div>
</aside>

<style>
	.premium-upsell {
		margin-top: 1.75rem;
	}
	.inner {
		padding: 1.15rem 1.2rem 1.25rem;
		border-radius: 16px;
		border: 1px solid #c7d2fe;
		background: linear-gradient(160deg, #eef2ff 0%, #ffffff 55%);
		box-shadow: 0 8px 28px rgba(37, 99, 235, 0.07);
	}
	.lead {
		margin: 0 0 0.4rem;
		font-size: 1.05rem;
		font-weight: 800;
		color: var(--ink);
		letter-spacing: -0.02em;
	}
	.sub {
		margin: 0 0 1rem;
		font-size: 0.92rem;
		line-height: 1.62;
		color: var(--ink-muted);
	}
	.sub strong {
		color: var(--ink);
	}
	.btn {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		padding: 0.62rem 1.2rem;
		border-radius: 999px;
		font-weight: 700;
		font-size: 0.9rem;
		text-decoration: none;
		background: var(--accent-dark);
		color: #fff;
		border: none;
		box-shadow: 0 4px 16px rgba(37, 99, 235, 0.25);
		transition:
			background 0.18s ease,
			box-shadow 0.18s ease;
	}
	.btn:hover {
		background: #1e40af;
		box-shadow: 0 6px 22px rgba(37, 99, 235, 0.32);
	}
	.note {
		margin: 0.85rem 0 0;
		font-size: 0.82rem;
		color: #6b7280;
		line-height: 1.5;
	}
	.note a {
		font-weight: 600;
		color: var(--accent-dark);
	}
</style>
